version: "3"
services:
  backend:
    build: ./backend
    restart: always
    ports:
      - "3000:3000"
  frontend:
    build: ./frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "4200:4200"